# Required Task 4 - unit tests.

**Given: *The rectangle*** (I) is defined by lines $x=\pm3$; $y=\pm4$. (We are considering that the rectangle includes its border lines)

The user inputs a pair of chars ($x1, y1$) from the keyboard. The system is required the pair of integer number. In this way, `a point A (II)` is defined.

**Required: a)** It is necessary to determine the relative position of the rectangle (I) *and the point A(II)*. The result should be output the console as text.

***Note:*** *in case the point A (I) belongs to the rectangle border it’s additionally required:*

*Clarify if the point A (I) belongs to the horizontal border, the vertical border or belongs to any rectangle corner.*

> Implement your java source to resolve the task. Numbers should be entered via the console

**b)** Perform the domain testing based on the meaning of the task.

Your decision form in the table, please. Choose and write the values that you will use for your future tests.

Write picked values under the table

**c)** Implement unit-tests for the all your java methods using unit tests frameworks (e.g. TestNG, Junit) and values selected through domain testing completed previously. Include parametrized tests into scope of your unit-tests.

